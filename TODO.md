# TODO

- [ ] Add structured trajectory logging format (`jsonl` + schema)
- [ ] Add dataset schema validators (required fields, type checks, versioning)
- [ ] Implement real OpenVLA checkpoint forward pass in adapter
- [ ] Add action post-processing layer for RT-style chunk outputs
- [ ] Add temporal horizon >1 rollout evaluator (sequence-level metrics)
- [ ] Add split-aware train script with optional validation metrics
- [ ] Add experiment tracking sink (W&B/MLflow abstraction)
